{
  "name": "todo-serverless-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for serverless Todo app",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0"
  }
}